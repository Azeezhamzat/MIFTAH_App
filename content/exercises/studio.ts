// Exercises hand-authored through the in-app Content Studio editor
// (/studio) land here, appended by src/app/api/studio/create-exercise. Kept
// separate from the thematic exercises/*.ts files (foundations, nominal,
// verbal, ...) so the editor never has to guess which theme a new exercise
// belongs to, and so its writes never touch files someone may be editing
// by hand at the same time.
import type { ExerciseSeed } from '../exerciseTypes';

export const studioExercises: ExerciseSeed[] = [];
